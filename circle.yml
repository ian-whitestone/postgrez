machine:
  python:
    version: 3.5.0

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - python -m pytest --verbose --cov=postgrez

  push:
    - coverage report
    - coveralls
